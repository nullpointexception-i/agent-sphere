{
  "name": "@buukle/agent-sphere-copilot-widget",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Embeddable chat widget that connects to the AgentSphere backend via OIDC SSO + REST timeline (typed log: user/assistant/tool/clarification/subagent/run_status) streamed over SSE.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && vite build --config vite.auth.config.ts",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "marked": "^15.0.12",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.5.0",
    "rollup-plugin-visualizer": "^7.1.1",
    "typescript": "^5.8.0",
    "vite": "^6.0.0"
  }
}
